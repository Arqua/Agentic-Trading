Cached OHLCV CSVs land here (git-ignored — they're just a local cache of
Yahoo Finance data, not part of the deliverable). Regenerate with:

```bash
python data.py
# or: python run_backtest.py --refresh
```
